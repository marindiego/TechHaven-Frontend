import Login from '@/components/Login';
import React from 'react';

const page = () => {
    return (
        <section id="login-page" className='bg-slate-100'>
            <Login />
        </section>

    );
};

export default page;