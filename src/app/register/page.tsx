import React from 'react';
import Register from '@/components/Register';

const page = () => {
    return (
        <section id="register-page" className="bg-backgroundColor p-1">
            <Register />
        </section>
    );
}
export default page;